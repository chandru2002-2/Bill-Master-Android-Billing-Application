<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for PDF Storage -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- ✅ ADDED: Required for Android 11+ to detect if WhatsApp is installed -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" /> <!-- Support for WhatsApp Business -->
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsBill">

        <!-- All Project Activities -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".PreviousBillsActivity"
            android:exported="false" />
        <activity
            android:name=".BillReviewActivity"
            android:exported="false" />
        <activity
            android:name=".CustomerDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ManualEntry"
            android:exported="false" />
        <activity
            android:name=".BillEntry"
            android:exported="false" />

        <!-- Launcher Activity (Login) -->
        <activity
            android:name=".loginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             FileProvider: Crucial for opening PDFs from Internal Storage
             authorities="${applicationId}.provider" matches the code in
             PreviousBillsActivity and BillReviewActivity.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

    </application>

</manifest>